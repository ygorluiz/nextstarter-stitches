import { calculateScale } from "./helper";

const spaces = {
  0: calculateScale(0),
  1: calculateScale(1),
  2: calculateScale(2),
  3: calculateScale(3),
  4: calculateScale(4),
  5: calculateScale(5),
};

export { spaces };
